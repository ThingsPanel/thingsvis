# ThingsVis Studio 开发环境配置

# API 基础 URL（可选）
# 在嵌入模式下会自动使用宿主页面的域名
# 独立运行时可以设置为 ThingsPanel 的代理路径进行测试
# THINGSVIS_API_URL=http://localhost:5002/thingsvis-api

# 独立运行时使用默认的本地服务器
# THINGSVIS_API_URL=http://localhost:3001/api/v1
