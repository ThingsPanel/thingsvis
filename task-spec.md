# 任务规范 (Task Specification)

## 边界与目标
1. 删除 `LineConnectionTool.tsx` 文件中残留的黄色高亮 Debug 提示框（文本为 "Line selected: ..."）。

## 子任务
- [ ] Sub-task 15: 在 `LineConnectionTool.tsx` 中剔除黄色背景的 Debug DOM 节点。

## 验收标准
- 选中连线组件后，左上角不再出现任何带有底色为黄色（即 `background: 'yellow'`）的脱离业务提示文本块。
