# {{COMPONENT_ID}}

This is a ThingsVis plugin package generated by `vis-cli`.

## Dev

```bash
pnpm dev
```

This serves `remoteEntry.js` for Module Federation.

## Visual test

Open the host app’s plugin spec runner and select `{{COMPONENT_ID}}`.


