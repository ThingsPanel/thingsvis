# TASK-04：首页改造 — 移除营销页

> **优先级**：🔴 P0
> **预估工时**：5分钟（最低限度）~ 4-6小时（推荐方案）
> **前置依赖**：无

---

## 背景

`HomePage.tsx`（309 行）是营销着陆页，包含"企业版/价格"导航。**开源项目不应有。**

---

## 方案选择

| 方案 | 改动量 | 效果 |
|------|--------|------|
| **最低限度**：`/` 重定向到 `/editor` | 1 行代码 | 登录后直达编辑器 |
| **推荐**：新建 `ProjectListPage` | 4-6h | 项目卡片列表 + 新建按钮 |

**最低限度方案：**

```tsx
// App.tsx
- <Route path="/" element={<HomePage />} />
+ <Route path="/" element={<Navigate to="/editor" replace />} />
```

---

## 任务清单

- [ ] **P0** 修改 `App.tsx` 路由（重定向到 `/editor`）
- [ ] **P0** 删除 `HomePage.tsx` 文件
- [ ] **P1** 新建 `ProjectListPage.tsx`（可选，v0.2.0 做）

---

## 验收标准

1. 访问 `/` 自动跳转到编辑器页面
2. 无"企业版"/"价格"等营销内容
